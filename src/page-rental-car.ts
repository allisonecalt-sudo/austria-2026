// Rental-car page bootstrap. Currently static markup + notes widget.
import { initNotesWidget } from './notes-widget.js';
import { initChatPlanPopup } from './popup-chat-plan.js';

initNotesWidget();
initChatPlanPopup();
