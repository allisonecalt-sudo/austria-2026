{
  "name": "austria-2026",
  "version": "0.1.0",
  "description": "Allison + Avital — Salzburg July 24-31 2026. Two trip-itinerary options with live note feedback.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173",
    "check:privacy": "node scripts/privacy-check.mjs",
    "check:links": "node scripts/link-check.mjs",
    "check:links:net": "node scripts/link-check.mjs --net",
    "lint": "eslint . --ext .ts && npm run check:privacy && npm run check:links",
    "lint:fix": "eslint . --ext .ts --fix",
    "format": "prettier --write \"src/**/*.{ts,html,css}\" \"*.{html,json,md}\"",
    "format:check": "prettier --check \"src/**/*.{ts,html,css}\" \"*.{html,json,md}\""
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.3.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
